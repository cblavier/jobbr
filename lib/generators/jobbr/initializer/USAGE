Description:
    Generates a jobbr initializer to require various dependencies.
    Will also create a Whenever configuration file.

Example:
    rails generate scheduled_job MySample

    This will create:
        Config: config/initializers/jobbr.rb
